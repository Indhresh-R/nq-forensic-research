# Conclusion — 24D Vol-Harvest Structure

## Verdict

**`C` on HIGH-conditional monetization** (IS `IS_PARTIAL` → Val `VAL_WIDE_DOMINATES`).  
OOS not required to close the HOW claim; optional for triple parity only.

**Precise answer to the HOW branch core question:**

> Strategy 12's activity/expansion signal, while real (A\*), does **not** translate into
> monetizable value through sizing (24A), stop/target **width**, or regime-filtering of a
> direction-agnostic breakout structure under realistic costs (24D). Wider stops help
> *unconditionally* as a general execution-parameter effect — not because of HIGH.

## Two independent questions (multiplicity)

| # | Question | Answer |
|---|----------|--------|
| Q1 | Does width help vs narrow? (`uncond_wide` − `uncond_base`) | **Yes** (IS +5.08; Val +5.12) — general parameter effect |
| Q2 | Does HIGH-conditioning beat always-wide? (`regime_width` − `uncond_wide`) | **No** (IS −3.10 [−3.56, −2.64]; Val **−2.79 [−3.53, −2.12]**) |

Q2 is the decisive HOW cell. Four policy arms; only these two contrasts are independent.

## Headline triple (decisive cell)

| Split | wide E | wide Sharpe | regime Sharpe | ΔSharpe (regime − wide) |
|-------|--------|-------------|---------------|-------------------------|
| IS | −1.24 | −3.17 | −6.27 | **−3.10** [−3.56, −2.64] |
| Val | −1.74 | −2.17 | −4.96 | **−2.79** [−3.53, −2.12] |

Best policy (`uncond_wide`) remains **E < 0** on IS and Val — not tradeable.

## 24B folded here (no separate ticket)

`regime_width` **is** the 24B hypothesis (stop/target distance conditional on regime),
tested as a side-by-side arm. Val confirms unconditional width dominates. Opening a
standalone 24B dossier would re-derive the same null — **skipped** (same discipline as
not duplicating 23D legs after pattern repeat).

## What remains

| Ticket | Status |
|--------|--------|
| 24A sizing | **C CLOSED** |
| 24B regime stop/target | **Absorbed / closed via 24D** |
| 24C holding period | **Held** — only remaining distinct mechanism; unlock after this Val review |
| 24D structure | **C** on HIGH harvest; width-parameter secondary finding documented |

## Failure mode (specific)

Symmetric breakout is fade-prone / cost-dominated (win ~27–40% under 1.0R/1.5R).
Widening stops/targets reduces premature stop-outs **in all regimes**, so always-wide
beats HIGH-aware width. Filtering to HIGH alone does not create +EV. The motivating
idea “use Strategy-12 HIGH to set width better” is not what drives the Q1 improvement.
