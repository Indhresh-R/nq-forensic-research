# 49. Daily directional streak persistence

Information test only. Uses Strategy 48's Globex session builder and does not modify that study.

```bash
python strategies/49_daily_streak_persistence/code/test_streaks.py
python strategies/49_daily_streak_persistence/code/run_streaks.py
```

The frozen rules are in `PREREGISTRATION.md`. The verdict is in `REPORT.md`.
