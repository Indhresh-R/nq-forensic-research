# Purpose — Engine sanity (not a market hypothesis)

Confirm the local NQ continuous pipeline and simple ledger math are correct
before attributing failed research edges to “the market.”

```text
If buy-and-hold ≠ last − first  → engine / data bug
If SMA hand ≠ rolling SMA       → indicator bug
If cross dates ≫ ^NDX           → calendar / daily aggregation bug (or roll noise)
```

This dossier does **not** claim tradable edge and is **outside** the
direction-resolution lock (13–22).
