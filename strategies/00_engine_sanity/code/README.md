# Code — 00_engine_sanity

From repo root:

```text
python strategies/00_engine_sanity/code/run_nq_engine_sanity.py
```

Requires local `data/nq_1m_continuous.parquet`. Optional: `yfinance` for ^NDX cross comparison.
