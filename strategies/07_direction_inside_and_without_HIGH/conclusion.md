# Conclusion — Direction Inside / Without HIGH

## Verdict

**`C` / `B` kill** — no promotable short-horizon directional layer. Stop inventing OHLC directional families.

## Why (numbers)

Inside HIGH, baseline FOLLOW is ~**51–55%**. Only single-clock leftovers (e.g. `on_open_continue` IS **56.2%**). ES/volume/multiscale and NO-HIGH serial/extreme/failed/retrace: multi-clock strong = **0**. Best NO-HIGH spikes (~55–58% IS) fail year/clock stability (e.g. extreme fade 2026 **51.9%**; failed-move 2026 **50.0%**).

## Split snapshot

| Split | Result |
|-------|--------|
| IS | ~50–58% spikes, not multi-clock |
| Validation | ~50–61% unstable |
| OOS | ~50–59% / year flips |

## What not to do next

- Do not stack filters on HIGH to “find” direction
- Do not reopen dead NQ price-pattern / ES / volume families under this protocol
