# Hypothesis — Pre-HIGH displacement / initiative

## Intuition

```text
I think direction can be inferred from which side already showed unusual
displacement in the window leading into Strategy-12 HIGH, because that
initiative is the imbalance the expansion is likely to continue resolving —
not because “momentum always works,” but because only *unusual* pre-event
displacement carries signed information.
```

```text
[T-W, T] displacement / path initiative
        |
   Is it unusual? (IS p66 of |x|/psr)
        |
       Yes → side = sign(initiative)
        |
Strategy-12 HIGH vs ALL
        |
Conditional lift?
```

## Frozen mechanics

| Item | Value |
|------|-------|
| W | 30 minutes |
| `disp` | close_T − close_{T−W} |
| `disp_psr` | disp / psr |
| `path_imb` | Σup − Σ\|down\| over closes in (T−W, T], / psr |
| Unusual | \|metric\| ≥ IS p66 at (session, T_offset), fit on IS only |
| Resolvers | `disp_follow`, `path_follow` (skip if not unusual) |
| Outcomes | H=30, H=60, SESS_END |
| Gate | Strategy 12 HIGH terciles untouched |

## Success / failure

Win-lift only: HIGH−ALL ≥ +3pp IS soft / +5pp strong; same-sign Val+OOS; multi-clock.
Kill cleanly — do not retune W or p66.
