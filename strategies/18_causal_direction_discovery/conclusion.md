# Conclusion — Causal direction discovery

## Verdict

**`C_null` (practical)** — no trustworthy WHICH WAY lead in this frozen menu.

The automated gate printed a fragile `B_lead` on `prior_sess_mid` / NY_PM
(multi-clock soft via **E**-lift). Inspecting win-lifts: several “strong”
cells have **negative OOS win lift**. That is not a promote.

## What the data show

### 1. Activity ≠ direction

Inside Strategy-12 HIGH, P(forward up) at H30 is ~**49–53%** across sessions
(IS). Knowing WHEN does not encode UP vs DOWN.

### 2. Frozen menu (causal, pre-specified)

Momentum, VWAP, extension-fade, prior-**session** mid, gap, path imbalance —
**none** clear a clean multi-clock **win-lift** Val+OOS story.

Medians of best-IS clocks (H30): lifts mostly **~0–2pp** IS and often flip
or die OOS.

### 3. What we did *not* do

- No post-hoc feature invention after seeing OOS
- No retuning Strategy 12
- No stops/targets mining

## Honest answer to “can you find direction in the data?”

**With the information set we used (NQ 1m path + session structure + prior
session/day levels), no stable causal direction resolver showed up next to
the activity gate.**

That is a useful negative result: the bottleneck is real, not just bad luck
with 16A/16B/17.

## What would be needed next (if continuing)

New **signed** information not already in this menu / killed dossiers, e.g.:

- lagged options / positioning (causal lag only)
- auction / imbalance prints if available
- a *different* trap definition with win-lift-only gates

Or stop searching for direction on top of Strategy 12 and treat it as a
regime feature only.
