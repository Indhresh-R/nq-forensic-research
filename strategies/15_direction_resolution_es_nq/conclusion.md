# Conclusion — Direction resolution ES/NQ

## Verdict

**`B→kill`** — not a trade. Soft/single-clock lift leftovers only (**0** strong;
**8** soft). No multi-clock year-stable conditional lift.

## Why (numbers)

Panel **195,058**. Resolvers: `follow_es`, `rs_continue`, `agree_follow`.

Headline best IS lift (H30) is small and unstable:

| Session | Best resolver IS lift | Val lift | OOS lift |
|---------|----------------------|----------|----------|
| LONDON | +1.7pp (`rs_continue`) | −0.7pp | −0.5pp |
| NY_AM | +3.4pp (`agree_follow`) | **−2.5pp** | +0.4pp |
| ASIA | +2.4pp (`follow_es`) | **−5.3pp** | +6.1pp |

London (strongest activity regime) shows ~**no** useful HIGH−ALL lift. Soft
candidates do not clear year-stable strong bars.

## Interpretation

ES/NQ signed information does **not** resolve direction better inside the
Strategy-12 activity regime than unconditionally — the interaction test fails.
Aligns with Strategy 07’s inside-HIGH cross-asset kill; this pass makes the
unconditional contrast explicit.

## What not to do next

- Do not expand ES variant grids (`es_last5`, disagree stacks, etc.)
- Do not promote soft single-clock cells
- Next Strategy 16+ must use a **different** signed-information source
  (trapped side, inventory, lagged options, etc.)
