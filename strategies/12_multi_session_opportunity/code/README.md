# Code — 12_multi_session_opportunity

From repo root:

```text
python strategies/12_multi_session_opportunity/code/run_nq_multi_session_opp.py
```

Artifacts land under `artifacts/12_multi_session_opportunity/` via `common.paths.art`.

Shared loaders: `common.sessions` / `common.nq_session` (`load_nq`, `build_session_facts`, `state_at_T_session`).
