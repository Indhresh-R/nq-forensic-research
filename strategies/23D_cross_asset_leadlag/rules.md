# Rules — 23D (frozen)

1. Overnight RS window: prior RTH close (≤16:00) → 09:30 open only.
2. Signals: `es_follow` / `es_fade` only (ZN/ZB unavailable this run).
3. Session: **NY_AM** only; clocks {15,30,60,90}; H {15,30,60}.
4. Grid = **24** cells — no expansion after look.
5. HIGH = Strategy-12 frozen p66; never refit.
6. Three-way reported for every result cell.
7. Kill family on clean fail — no ZN/ZB rescue after ES fail; no 23E without updating `direction_resolution.md`.
