# Strategy 55 — Price-Path Event Screen

**Status:** Event A **KILL_AFTER_TRADE** (complete). See `EVENT_A_COMPLETE.md`.

Next independent event: Strategy 56 Range Break → Failed Return (prereg only).
