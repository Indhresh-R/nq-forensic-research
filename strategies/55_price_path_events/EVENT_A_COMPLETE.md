# Event A — COMPLETE / KILL

**Final:** `KILL_AFTER_TRADE`

Destination asymmetry (origin ≫ extension, ~−25 pp) was stable IS/Val/OOS.  
Fixed 15m against-impulse execution had ~0 gross / ~−1 net after 1.0 pt RT.

**Do not retune. Do not fix with filters. Do not combine with Event B.**

Next independent event: Strategy 56 Range Break → Failed Return (`../56_range_break_failed_return/`).
